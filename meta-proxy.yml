zookeeper:
  address: [c3cloudsrv.zk.hadoop.srv:11000]
  root: /pegasus/c3tst-metaproxy
  timeout: 1000 # ms
  table_watcher_cache_capacity: 1024

metric:
  type: falcon
  tags: # custom config // if type=falcon, the config must set into "perfcounter.json" because of falcon sdk standards
    region: c3tst_staging
    service: meta_proxy
